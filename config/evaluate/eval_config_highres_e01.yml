#optional: if commented out all is taken care of by the default settings
# NB. global options apply to all run_ids 
global_plotting_options:
  regions: ["europe"]
  image_format : "png" #options: "png", "pdf", "svg", "eps", "jpg" ..
  dpi_val : 300
  fps: 2
#  ERA5:
#    marker_size: 2
#    scale_marker_size: 1
#    marker: "o"
#    # alpha: 0.5
#    2t: 
#      vmin: 250
#      vmax: 300
#    10u:
#      vmin: -40
#      vmax: 40

evaluation:
  metrics  : ["rmse", "mae"]
  regions: ["europe"]
  summary_plots : true
  ratio_plots : false
  heat_maps : false
  summary_dir: "/leonardo_scratch/large/userexternal/clussana/plots/"
  #  plot_ensemble: "members" #supported: false, "std", "minmax", "members"
  plot_score_maps: false #plot scores on a 2D maps. it slows down score computation 
  print_summary: false #print out score values on screen. it can be verbose
  log_scale: false
  add_grid: false
  score_cards: false
  bar_plots: false
  num_processes: 0  #options: int, "auto", 0 means no parallelism (default)
  # baseline: "ar40mckx"


default_streams:
  MEPS:
    channels: ["2t"] #, "10v", "z_500", "t_850", "u_850", "v_850", "q_850", ]
    evaluation:
      forecast_step: "all"
      sample: "all"
      ensemble: "all" #supported: "all", "mean", [0,1,2]
    plotting:
      sample: "all"
      forecast_step: "all"
      #      ensemble: [0,2,5] #supported: "all", "mean", [0,1,2]
      plot_maps: true
      plot_bias: false
      plot_target: false
      plot_histograms: true
      plot_animations: true
      
run_ids :
  t2k7s5nm:
    label: "pretrained model e83qomjp (ERA5, MEPS, synop)"
    #    results_base_dir : "/leonardo_work/AIFAC_5C0_154/weathergen/shared_work/results/s1sjfue2/"
    #NEW: if "streams" is not specified, the default streams are used
    
      
  h42qskty:
    label: "pretrained model e83qomjp (MEPS, synop)"
    #    results_base_dir : "/leonardo_work/AIFAC_5C0_154/weathergen/shared_work/results/s1sjfue2/"
    #NEW: if "streams" is not specified, the default streams are used
