version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"

python:
  install:
    - requirements: packages/evaluate/docs/requirements.txt

mkdocs:
  configuration: packages/evaluate/mkdocs.yml
